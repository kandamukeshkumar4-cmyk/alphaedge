## Summary
- Fixes portfolio positions aggregation (GROUP BY slug/side) and mark pricing from forecasts.
- Aligns portfolio API schema, tests, and `/portfolio` UI with paper trading display.

## Merge order
- **Merge after Loop A** (`feat/deploy-health`) and **before Loop B** in A → C → B → F → D → E.

## Test plan
- [ ] `cd backend && uv run --extra dev pytest tests/test_portfolio.py -q`
- [ ] Verify portfolio API returns net positions per market side.
- [ ] Smoke-check portfolio dashboard marks and totals.
