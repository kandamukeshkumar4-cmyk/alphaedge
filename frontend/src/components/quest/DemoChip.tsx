export function DemoChip() {
  return (
    <span
      className="rounded bg-secondary-dim px-1.5 py-0.5 text-[9px] font-bold uppercase tracking-wide text-secondary"
      title="Sample data shown because the backend has no data yet. Start the API + workers for live content."
    >
      demo
    </span>
  );
}
