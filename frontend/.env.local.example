# Point local Next.js at the live HF Space API (no Docker required).
NEXT_PUBLIC_API_URL=https://mukeshkumar007-alphaedge-api.hf.space

# Optional: use a local backend instead (requires docker compose up).
# NEXT_PUBLIC_API_URL=http://localhost:8000
