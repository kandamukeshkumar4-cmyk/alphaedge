# PWA icons

Install icons currently use `icon.svg` (Astryx charcoal + mint mark).

Maskable PNG sizes (192x192, 512x512) are not fabricated in this loop — generate them from `icon.svg` in a design pass and add entries to `src/app/manifest.ts`.
