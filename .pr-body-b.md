## Summary
- Adds market resolution admin API, Alembic migration, and settlement flow.
- Extends market detail schema/UI and trade panel for resolved markets.
- Includes pytest coverage in `test_market_resolution.py`.

## Merge order
- **Merge after Loop C** (`feat/portfolio-positions`) in A → C → B → F → D → E.

## Test plan
- [ ] `cd backend && uv run --extra dev pytest tests/test_market_resolution.py -q`
- [ ] Run migration `013_market_resolutions` on dev DB.
- [ ] Admin resolve flow updates market state and UI reflects resolution.
