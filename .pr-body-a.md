## Summary
- Adds detailed deploy health API (`health.py` schemas + tests) for backend readiness checks.
- Registers health router in `main.py`.
- Adds frontend `HealthBanner` and layout wiring plus `.env.local.example` notes.

## Merge order
- **Merge first** in the A → C → B → F → D → E stack (Loop A).

## Test plan
- [ ] `cd backend && uv run --extra dev pytest tests/test_health_detailed.py -q`
- [ ] `GET` detailed health endpoint returns expected JSON.
- [ ] Frontend shows health banner when API reports degraded state.
