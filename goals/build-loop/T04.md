# T04 — Alignment scorer (trigger)

Spec: docs/project/BUILD_LOOP_BACKEND.md §3 T04.
Status: DONE (Cursor verify 2026-07-02 iter 11).

## Review (Cursor, 2026-07-02)

**Verdict: PASS**

Gate: **525 passed, 5 skipped** (shared with T03 verify run); ruff clean.

Guardrails §0: PASS — trigger only, model layer is feature input not order path.

Spec compliance:
- `signals/alignment.py`: rolling window, layers, weighted score, `analyst.trigger` emit ✓
- Tests: 2 layers → no trigger; 3 same direction → trigger; mixed → no; dedupe; window expiry (`test_alignment.py`) ✓
- Wired from `runner._run_diff_engine` when `alignment_enabled` ✓
- `persist_alignment` → `signal_events` type `alignment` + `analyst.trigger` DomainEvent ✓

Accept (e2e trigger once): `test_dedupe_fires_once_within_window` + `test_persist_alignment_writes_signal_and_trigger` ✓

### Non-blocking nit
- Model layer uses optional `model_vote` kwarg in scorer; live predictor hook not wired in hot path yet (T07 will consume triggers). Layer enum reserved for T05/T06 deltas.
