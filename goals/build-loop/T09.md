# T09 — Alert dispatch

Spec: docs/project/BUILD_LOOP_BACKEND.md §3 T09.
Status: DONE — **PASS** (2026-07-02 iter 17 re-verify).

## Review (Cursor, 2026-07-02 iter 17)

**Verdict: PASS** — gate blocker fixed; T09 scope green.

Pytest: **604 passed, 5 skipped** ✓  
Ruff: **FAIL** — `app/ml/trainer.py:8` unused `XGBClassifier` import (T11 WIP in shared tree)

Spec review (code-only, pending green gate):
- `alert_dispatch.py`: alerts table + WS `alerts` + optional Telegram/webhook (OFF default) ✓
- `test_alert_dispatch.py`: disabled flags → zero transport calls; dedupe; ≤400 char telegram ✓
- Wired from `persist_alignment` + `analyst.persist_publish` ✓

### CONFIRMED finding
1. **Gate red** — `uv run --extra dev ruff check app tests` fails on T11-owned `trainer.py`. Fix unused import (or isolate T11) before DONE.

→ Status back to **CLAIMED-OPUS** for gate fix, or Opus cleans T11 lint while building T11.
