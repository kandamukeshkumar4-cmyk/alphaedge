# T07 — Analyst agent (briefs + claims)

Spec: docs/project/BUILD_LOOP_BACKEND.md §3 T07.
Status: DONE (Cursor verify 2026-07-02).

## Review (Cursor, 2026-07-02)

**Verdict: PASS**

Gate: **567 passed, 5 skipped**; ruff clean; migration `025_analyst_briefs` chains `024→025`.

Guardrails §0: PASS — no order path; LLM writes prose only; claim is deterministic (`extract_claim`); sentiment/news never standalone triggers.

Spec compliance:
- `agents/analyst.py`: gather → evidence → brief → claim → persist; LangGraph-optional; fallback without API key ✓
- `schemas/brief.py`: ≥1 citation enforced (`test_citation_required_rejection`) ✓
- Migration 025: `analyst_briefs` + `brief_claims` ✓
- Wired from `persist_alignment` → `run_analyst_for_trigger` with cooldown ✓
- `record_brief_generated` metrics hook ✓
- WS hub publish + persist (`test_analyst_agent`, `test_briefs_persist`) ✓

Accept (fixture trigger → brief with citation + claim): `test_run_analyst_fallback_generator_persists_brief` ✓

### Non-blocking nits
- Cooldown is in-process only (resets on restart) — fine for paper MVP.
- Alembic head may advance to 026 when T08 lands — chain from 025 for later tickets.
