# T03 — Snapshot diff engine

Spec: docs/project/BUILD_LOOP_BACKEND.md §3 T03.
Status: DONE (Cursor verify 2026-07-02 iter 11).

## Review (Cursor, 2026-07-02)

**Verdict: PASS**

Gate: **525 passed, 5 skipped**; ruff clean; no new migration (correct).

Guardrails §0: PASS — derived signals only, no order path, no LLM in compute path.

Spec compliance:
- `signals/diff_engine.py`: `compute_market_delta`, `DeltaKind`, `DiffEngineService`, `persist_deltas` → `signal_events` + `DomainEventBus` ✓
- Boundary tests: exactly-at vs strictly-over, seed/no-prev, idempotent (`test_diff_engine.py`) ✓
- Wired: `runner._persist_tick_event` → `_run_diff_engine` on every stream event ✓
- Config thresholds: `diff_price_jump_bps`, etc. ✓

Accept (stream ticks through diff engine): `runner.py:144-145` calls `_run_diff_engine` after tick persist ✓

### Non-blocking nit
- Spec names Redis store (`diff:{slug}`, TTL 1h); implementation is in-memory only. `diff_state_backend` config exists but unused in `get_diff_engine()`. OK for single-process paper trading; implement Redis before multi-worker deploy.

### Out of scope (T04 already in tree)
- `alignment.py` wired from `_run_diff_engine` — verify at T04 IN-REVIEW.
