# T05 — Whale tracker (Polymarket data-api)

Spec: docs/project/BUILD_LOOP_BACKEND.md §3 T05.
Status: DONE (Cursor verify 2026-07-02 iter 13).

## Review (Cursor, 2026-07-02)

**Verdict: PASS** (with documented spec deviation)

Gate: **555 passed, 5 skipped**; ruff clean; migration `024_wallet_position_snapshots` chains `023→024`.

Guardrails §0: PASS — read-only data-api, no keys/wallets/execution, whale signals feed alignment only.

Spec compliance:
- `polymarket_data_api.py` + fixture tests ✓
- `qualify_whale` boundaries + one-hit-wonder (`test_whale_qualification.py`) ✓
- Position diff → `whale_delta` + alignment path (`test_whale_deltas.py`) ✓
- `WhaleTrackerService`, ARQ tasks in `workers/tasks.py` ✓
- `wallet_position_snapshots` table + migration 024 ✓

Accept (qualified wallet add → whale_delta → scorer): `test_snapshot_and_diff_emits_whale_delta` ✓

### Accepted deviation (truth-first)
- Spec said delete `onchain.py` (stub). It is a **working** read-only subgraph connector (`wallet_service`, `test_data_connectors`). Opus correctly **kept** it and added data-api as whale source. Recorded in STATE decisions.

### Non-blocking nit
- Live data-api field shapes are best-effort; monitor before trusting production whale signals.
