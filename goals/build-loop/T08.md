# T08 — Eval harness (claim grading)

Spec: docs/project/BUILD_LOOP_BACKEND.md §3 T08.
Status: DONE (Cursor verify 2026-07-02 iter 15).

## Review (Cursor, 2026-07-02)

**Verdict: PASS**

Gate: **597 passed, 5 skipped**; ruff clean; migration `026_analyst_eval_aggregates` head (chains `025→026`).

Guardrails §0: PASS — grading reads snapshots only; void when insufficient data; no order path.

Spec compliance:
- `eval/claim_scorer.py`: 4 directions × correct/incorrect/void matrix + epsilon boundaries ✓
- **No-lookahead negative control:** `test_no_lookahead_ignores_post_horizon_snapshot` ✓
- `eval/analyst_metrics.py`: accuracy/Brier, provisional `<30` (`test_analyst_metrics.py`) ✓
- `eval/citation_audit.py`: structural post-hoc citation check ✓
- ARQ: `score_claims_task` (15min) + `analyst_aggregates_task` (hourly) ✓
- `record_claim_scored` hook available for metrics ✓

Accept (seeded fixture briefs → deterministic grades): service tests in `test_claim_scorer.py` ✓

Hire-signal bar met — do not cut corners finding: none blocking.
