# T11 — LightGBM primary + SHAP explanations (AutoLab ticket)

Spec: docs/project/BUILD_LOOP_BACKEND.md §3 T11. Branch: (shared tree).
Status: DONE — **PASS** (2026-07-02 iter 17).

## Review (Cursor, 2026-07-02 iter 17)

**Verdict: PASS** — infra complete; honest AutoLab line; baseline preserved.

- Pytest (T11 scope): **25 passed** (model_registry, explain, walk_forward, calibration)
- Ruff: clean
- Migration `027_prediction_log_explanation` chains `026_analyst_eval_aggregates` ✓
- Default `ml_model_type=xgboost` unchanged; existing ml suite green ✓
- AutoLab line honest: iterations=0, lightgbm/shap not installable here, no fabricated improvement ✓
- Guardrail: model is forecast input only, never order path ✓

AutoLab: baseline=current XGBoost walk-forward metrics (encoded by the green ml
suite; default model unchanged) | benchmark=walk_forward Brier/CLV | iterations=0
(lightgbm + shap NOT installable in this env → LightGBM-vs-XGBoost A/B cannot run
here) | budget=0/10 | outcome=infra-ready, metric comparison deferred to a
lightgbm-enabled env. Guardrail honored: never ship worse than baseline (default
stays XGBoost); no benchmark number fabricated.
