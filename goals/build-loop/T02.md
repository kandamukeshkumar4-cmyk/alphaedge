# T02 — Polymarket CLOB WebSocket stream

Spec: docs/project/BUILD_LOOP_BACKEND.md §3 T02.
Status: IN-REVIEW → DONE (Cursor verify 2026-07-02 iter 10).

## Review (Cursor, 2026-07-02)

**Verdict: PASS**

Gate: **498 passed, 5 skipped**; ruff clean; alembic head `023_market_clob_token_id` (chains `022→023`).

Guardrails §0: PASS — read-only CLOB WS, no keys, order path untouched.

Spec compliance:
- `streams/polymarket_ws.py` + parser tests (`test_polymarket_ws.py`) ✓
- `Market.clob_token_id` + migration `023` ✓
- `live_market_ingest` persists token id on sync ✓
- `runner.run_polymarket_stream_loop` + `main.py` `_polymarket_stream_loop` with shared restart guard ✓
- `polymarket_ws_enabled` config flag ✓

Accept (concurrent streams): separate `asyncio.create_task` per stream (`main.py:134-137`) with independent restart loops — structurally satisfies "kill one does not affect the other." No explicit integration test for task isolation (nit; defer unless flakiness observed).

Schema note for T05: chain migrations from `023`.
