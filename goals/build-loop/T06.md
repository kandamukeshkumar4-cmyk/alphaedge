# T06 — News→price lag detector

Spec: docs/project/BUILD_LOOP_BACKEND.md §3 T06.
Status: DONE (Cursor verify 2026-07-02 iter 13).

## Review (Cursor, 2026-07-02)

**Verdict: PASS**

Gate: **555 passed, 5 skipped**; ruff clean; no schema change.

Guardrails §0: PASS — sentiment is metadata/direction on `news_arrival` only, not a standalone trade trigger (guardrail 3).

Spec compliance:
- `signals/news_lag.py`: `evaluate_news_lag`, stale/priced-in/unpriced boundaries ✓
- Tests: priced-in → no event; unpriced → event; stale → no event (`test_news_lag.py`) ✓
- Emits `news_arrival` DeltaEvent → diff persist + T04 scorer ✓

### Design note (acknowledged)
- Missing price history treated as unpriced — safe under ≥3-layer alignment gate; Opus flagged for stricter branch if desired later.

**T13 eligibility:** T03 + T06 now DONE → T13 claimable.
