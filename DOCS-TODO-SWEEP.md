# DOCS-TODO-SWEEP

## Broken doc links

No broken relative links found across 84 .md files (all `docs/**/*.md` and `*.md` in repo root). Every markdown link (inline, reference-style, image, autolink) resolves to an existing file. The one regex match `[factor](features)` inside `SCOUT105-PROVENANCE.md` is Python code inside a fenced code block, not a real link.

## TODO/FIXME inventory

### backend/app/

No TODO, FIXME, or XXX markers found.

### frontend/src/

No TODO, FIXME, or XXX markers found. The UUID template string `4xxx` in `frontend/src/lib/paper-trading-api.ts:352` is not a marker.

*Sweep generated 2026-07-25.*
